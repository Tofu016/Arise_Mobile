# expo-router bundles web-compatibility tooling (vaul/@radix-ui) that pulls
# in a react-dom version wanting a slightly newer React than what's pinned
# in this project — a known upstream quirk, not something specific to
# anything we've built. This tells npm to use the older, more forgiving
# peer-dependency resolution behavior instead of hard-failing on it.
legacy-peer-deps=true
